#lang racket
;This prefix is so this module doesn't use the #%module-begin #%top etc
;defined in those files.
(require (prefix-in ML- "../ML-lib/Fields/Fields3.rkt")
         "../ML-lib/ML-Helpers.rkt")
(provide (unprefix-out ML- "../ML-lib/Fields/Fields3.rkt"))


(setup-reader 'Fields3  #:let #:+ #:- #:* #:/
              #:> #:< #:>= #:<= #:== #:!= #:++
              #:if #:func #:reassign #:lambda #:record)