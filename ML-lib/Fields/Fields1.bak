#lang racket
(require (for-syntax syntax/parse))
(require "../ML-Helpers.rkt")
(require (prefix-in MV3- "../MutableVars/MutableVars3.rkt"))
(require "fields-common-base.rkt"
         "../MutableStructures/MutableStructuresSetup.rkt")
(require "../testPrinter.rkt")
(provide
 record
 reassign-field
 access
 (unprefix-out MV3- "../MutableVars/MutableVars3.rkt")
 (all-from-out "../testPrinter.rkt")
 )


;Mutable Structures all use these types of fields
;so this setup is used for all three of them, and this
;language too.
(setup-mutable-structures)
(setup-printer)